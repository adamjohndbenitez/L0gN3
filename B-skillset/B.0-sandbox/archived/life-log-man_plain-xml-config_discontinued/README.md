
Issue:
pom.xml is present That’s why:

IntelliJ can run it ✅ (it uses its own built-in Maven)
But in terminal, you can’t just do ./mvnw spring-boot:run ❌

project looks like:

Plain Java + Spring (XML config)
NOT Spring Boot

aj: I think I was just testing how Spring Framework build in boilerplate without Spring Boot Configuration.