package nubank.cosmo.solid.dip;

/**
 *
 * This is the Dependency Inversion Principle (DIP), which is about depending on abstractions (like interfaces), not on concrete details. High-level modules should not depend on low-level modules; both should depend on abstractions. This is why dependency injection is so popular-it allows for easily swapping out implementations, which is a huge benefit for testing.
 *
 *
 */
public class DependencyInversionPrinciple {
}
